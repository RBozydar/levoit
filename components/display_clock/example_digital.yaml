# display_clock - style: digital, self-contained 7-segment display.
# Compile:  esphome compile components/display_clock/example_digital.yaml

esphome:
  name: display-clock-digital

packages:
  base: !include common_base.yaml
  lvgl_colors: !include common_lvgl.yaml
  display: !include tft_big.yaml

lvgl:
  widgets:
    - display_clock:
        id: dc
        time_id: sntp_time
        width: 240
        height: 120
        align: CENTER
        style: digital
        foreground: cc_hands
        background: cc_bg
        show_seconds: false
        twenty_four_hour: true        # false = 1-12, add am/pm to digital and flipclock
        digital:
          segment_style: classic  # classic (tapered LCD) | rounded (capsule ends)
          blink: true
          blank_leading_zero: false
